<!doctype html>

<html class="no-js" lang="{{ request.locale.iso_code }}">

  <head>

    <meta charset="utf-8">

    <meta http-equiv="X-UA-Compatible" content="IE=edge">

    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />

    <meta name="theme-color" content="">
    
    
    <link rel="canonical" href="{{ canonical_url }}">

    <link rel="preconnect" href="https://cdn.shopify.com" crossorigin>

    {%- if settings.favicon != blank -%}

    <link rel="icon" type="image/png" href="{{ settings.favicon | img_url: '32x32' }}">

    {%- endif -%}

    {%- unless settings.type_header_font.system? and settings.type_body_font.system? -%}

    <link rel="preconnect" href="https://fonts.shopifycdn.com" crossorigin>

    {%- endunless -%}

    <title>{{ page_title }}</title>

    {% if page_description %}

    <meta name="description" content="{{ page_description | escape }}">

    {% endif %}

    {% render 'meta-tags' %}
    <link href="https://use.fontawesome.com/releases/v5.0.1/css/all.css" rel="stylesheet">
    
    {{ content_for_header }}

    {% include 'theme-style' %}
    

    <script>document.documentElement.className = document.documentElement.className.replace('no-js', 'js');</script>
	{% include 'theme-scripts' %}
    
  </head>

  <body class="template_{{ template |handle }}">
    
    <div class="wrapper">   

      <header id="headerSection">

        {% section 'announcement-bar' %}
        {% section 'header' %}      

      </header>
      
      {% include 'breadcrumb' %}      

      <main id="MainContent" class="content-for-layout focus-none" role="main" tabindex="-1">

        {{ content_for_layout }}

      </main>
      
      {% section 'footer' %}

      {% include 'search-drawer' %}
      
      {% render 'side-drawer'%}


      <ul hidden>

        <li id="a11y-refresh-page-message">{{ 'accessibility.refresh_page' | t }}</li>

      </ul>

    </div>
    
    {% render 'newsletter-popup'%}
    
    {% if settings.enable_cart_drawer and template != 'cart'%}
    
    {% include 'mini-cart' %}
    
    {% endif %}
    
    <style>
      form button[type="submit"]:not([name="checkout"]) {
        position: relative;
      }
      form button[type="submit"].is-loading svg, form button[type="submit"].is-loading span {
        display: none;
      }
      form button[type="submit"].is-loading svg.svg-loader {
        display: block !important;
        margin: auto !important;
        margin-top: 0;
        width:34px;height:34px;
      }

      .br_more_filter.hide .showLess {
        display: none;
      }
      .br_more_filter.show .showMore {
        display: none;
      }
      .accordion-container .set .panel-title.active .iconPlus,
      .accordion-container .set .panel-title .iconMinus{
        display: none;
      }
      .accordion-container .set .panel-title .iconPlus,
      .accordion-container .set .panel-title.active .iconMinus{
        display: block;
      }
    </style>
    
  </body>

</html>
