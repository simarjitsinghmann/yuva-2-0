{% comment %}
    Renders a pagination bar

    Accepts:
    - paginate: {Object} Paginate Liquid object (required)

    Usage:
    {% include 'pagination', paginate: paginate %}
{% endcomment %}
<ul class="list--inline pagination">
  {% unless paginate.previous.is_link %}
    <li aria-hidden="true">
      <button class="btn btn--tertiary btn--narrow" disabled>
        <span class="icon__fallback-text"><img src={{'carousel-left-arrow.svg' | asset_url}} alt='right-arrow'></span>
      </button>
    </li>
    {% else %}
    <li>
      <a href="{{ paginate.previous.url }}" class="btn btn--tertiary btn--narrow">
        <span class="icon__fallback-text"><img src={{'carousel-left-arrow.svg' | asset_url}} alt='right-arrow'></span>
      </a>
    </li>
  {% endunless %}
  <li class="pagination__text">
    {{ paginate.current_page}} of {{ paginate.pages }}
  </li>
  {% unless paginate.next.is_link %}
    <li aria-hidden="true">
      <button class="btn btn--tertiary btn--narrow" disabled>
        <span class="icon__fallback-text"><img src={{'carousel-left-arrow.svg' | asset_url}} alt='left-arrow'></span>
      </button>
    </li>
  {% else %}
    <li>
      <a href="{{ paginate.next.url }}" class="btn btn--tertiary btn--narrow">
        <span class="icon__fallback-text"><img src={{'carousel-right-arrow.svg' | asset_url}} alt='right-arrow'></span>
      </a>
    </li>
  {% endunless %}
</ul>
