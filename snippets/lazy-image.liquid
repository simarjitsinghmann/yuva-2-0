<img
     data-src="{{ image | img_url: 'master' }}"
     
     class="{{class}} lazy"
     {%- if max_width != blank -%}
     width="{{ max_width }}"
     {% else %}
     width="{{ image.width }}"
     {% endif %}
     >