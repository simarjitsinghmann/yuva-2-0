<div id="mini_cart_shipping" {% if settings.enable_shipping and settings.shipping_rate != blank %}data-shipping="{{settings.shipping_rate | times:100}}" data-shipText="{{settings.shipping_text}}"{% endif %}>
</div>
<script id="CartTemplate" type="text/template">
    {% raw %}
  <div id="mini__cartForm">
     <form action="{% endraw %}{{ routes.cart_url }}{% raw %}" class="cart__contents critical-hidden" method="post" id="cart" cart-form>
	<div class="cart-item-container">	
    	{% endraw %}
        {% if settings.enable_shipping and settings.shipping_rate != blank %}
        {% raw %}
    	{{#if shippPending }}     
		 <p>{{{shippPending}}}</p>
         {{else}}
          <p>{% endraw %}{{settings.free_shipping_text}}{% raw %}</p>
        {{/if}}
		 <div class="progress">
		  <div class="progress-bar" role="progressbar"
		  aria-valuemin="0" aria-valuemax="100" style="width:{{shipPendingPercentage}}">
			<span class="sr-only">{{shipPendingPercentage}} Complete</span>
  </div>
  </div>
        {% endraw %}
        {% endif %}
        {% raw %} 	
        <div class="cart-items-wrapper">
		{{#items}}
		<div class="media-link" data-line="{{line}}">
			<img src="{{img}}" alt="{{name}}" width="65" height="73">
			<div class="media-body">
				<h5 class="cart-item-link">
					<a href="{{url}}">{{name}}</a>
  </h5>
                  {{#if variation}}
                <span class="item-variant-title">{{variation}}</span>
                  {{/if}}
                  {{#if sellingPlan}}
                <span class="item-variant-title">{{sellingPlan}}</span>
                  {{/if}}
				<h6 class="product__price">
                  {{#if discountsApplied}}
                        <small class="item__price-strikethrough"><s>{{{originalLinePrice}}}</s></small>
                        <br><span>{{{linePrice}}}</span>
                      {{else}}
                        <span>{{{linePrice}}}</span>
                      {{/if}}
  </h6>
                {{#if discountsApplied}}
                      <div class="text-right">
                        {{#each discounts}}
                          <small class="item__discount">{{ this.title }}</small><br>
                        {{/each}}
  </div>
                  {{/if}}
				<div class="right-media-cart">
					<div class="quantity">
						<input type="number" class="ajaxcart__qty-num" name="updates[]" min="1" max="9" step="1" value="{{itemQty}}" data-id="{{key}}" data-line="{{line}}">
						<div class="quantity-nav">
							<div class="quantity-button quantity-up ajaxcart__qty--plus"  data-id="{{key}}" data-line="{{line}}" data-qty="{{itemAdd}}"><svg width="24px" height="24px" viewBox="0 0 24 24" role="img" xmlns="http://www.w3.org/2000/svg" aria-labelledby="plusIconTitle" stroke="#909097" stroke-width="4" stroke-linecap="round" stroke-linejoin="round" fill="none" color="#000000"> <title id="plusIconTitle">Plus</title> <path d="M20 12L4 12M12 4L12 20"/> </svg></div>
							<div class="quantity-button quantity-down ajaxcart__qty--minus" data-id="{{key}}" data-line="{{line}}" data-qty="{{itemMinus}}"><svg width="24px" height="24px" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg" fill="none" stroke="#909097" stroke-width="4" stroke-linecap="round" stroke-linejoin="round" class="feather feather-minus"><line x1="5" y1="12" x2="19" y2="12"></line></svg></div>
  </div>
  </div>
					<div class="sd_mini_removeproduct" line="{{line}}" >{%-endraw-%}{{'sections.cart.remove' | t}}{%-raw-%}</div>
  </div>
  </div>

  </div>
		{{/items}}	
  </div>
  </div>
	<div class="bottom-cart-box">
		<div class="leave-order-note">
			<span>{%-endraw-%}{{'sections.cart.note_heading' | t}}{%-raw-%}</span>
				<textarea class="form-control" rows = "3" name="note">{{{note}}}</textarea>
  </div>
		<ul>

         {{#totalCartDiscount}}
         	<li class="coupanName"><strong>{{title }}</strong></li>
            <li class="minicart__savings">{%-endraw-%}{{'sections.cart.discount' | t}}{%-raw-%} <strong>-{{price}}</strong></li>
          {{/totalCartDiscount}}
			<li class="totle-price">{%-endraw-%}{{'sections.cart.subtotal' | t}}{%-raw-%}<strong>{{{totalPrice}}}</strong></li>
  </ul>
		<small class="bottom-p">{%-endraw-%}{{'sections.cart.taxes_and_shipping_at_checkout' | t}}{%-raw-%} </small>
		<div class="cart-btn-container">
			<input type="submit" class="checkout-btn button black-btn" value="{%-endraw-%}{{'sections.cart.checkout' | t}}{%-raw-%}" name="checkout"/>
  </div>
			<a class="shopping_cont" href="/cart">{%-endraw-%}{{'sections.cart.view_cart' | t}}{%-raw-%}</a>
  </div>
  </div>
  </form>
  </div>
{% endraw %}
</script>
<script src="{{'cart-ajax.js' | asset_url}}" defer="defer"></script> 