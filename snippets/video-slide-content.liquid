{% if block.settings.height != 'use_image' %}
  <style>
    @media only screen and (max-width: 480px){
      [data-block-id="{{ block.id }}"] video{
        width: auto !important;
        height: 100% !important;
      }
    }
  </style>
  {% endif %}

  <div class="block--image fade-in-child preventOverflow home-video-sec {{ block.settings.height }} {{ block.settings.color }}" data-block-id="{{ block.id }}" data-block-type="hero" data-overlay-header>
    
    <video width="100%" height="750px" poster="{{block.settings.poster | img_url:'master'}}" autoplay="true" loop="true" muted="true" playsinline >
      
      <source src="{{ block.settings.videofile }}"type="video/mp4">
    </video> 
    <div class="videoOverlay"></div>
    <div class="container">    
    {% unless block.settings.link_text == blank and block.settings.title == blank and block.settings.description == blank %}
    {% if block.settings.link_text == '' and block.settings.link  %}
    <a href="{{ block.settings.link }}">
      {% endif %}

      <div class="flex-absolute {{ block.settings.text_position }} ">
        <div class="wrapper">
          {% if block.settings.title != blank %}
          <h1>{{ block.settings.title | escape }}</h1>
          {% endif %}
          
          {% if block.settings.subtitle != blank %}
         <h2>{{block.settings.subtitle}}</h2>
          {% endif %}
         {% if block.settings.description != blank %}
          <p>{{ block.settings.description }}</p>
         {% endif %}
          {% unless block.settings.link_text == blank or block.settings.link == blank%}
          <a class="{{ block.settings.button_style }}black-btn button" href="{{ block.settings.link }}">{{ block.settings.link_text | escape }}</a>
          {% endunless %}
        </div>
      </div>

      {% if block.settings.link_text == '' and block.settings.link %}
    </a>
    {% endif %}
    {% endunless %}
  </div>
  </div>
  <style>
    {% if block.settings.overlay %}
    .banner-{{block.id}} .home-video-sec .videoOverlay {
      position: absolute;
      top: 0; 
      left: 0;
      right: 0;
      bottom: 0;
      height: 100%;
      background: {{block.settings.bgcolor}};
      opacity:{{block.settings.opacity}}%;
    }
    {% endif %}
    .banner-{{block.id}} h1{
      color:{{block.settings.title_color}};
    }
    .banner-{{block.id}} h2{
      color:{{block.settings.subtitle_color}};    }
    
    
    .banner-{{block.id}} p{
      color:{{block.settings.desc_color}};
    }
     .banner-{{block.id}} .button {
    background-color:{{ block.settings.button_text_bk }};
    color: {{ block.settings.button_text_color }};
  }
  .banner-{{block.id}} .button:hover{
    color:  {{ block.settings.hvr_button_text_color }};
    background-color:{{ block.settings.hvr_button_text_bk }}
  }
  </style>