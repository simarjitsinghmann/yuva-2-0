<div class="container"> 
  <div class="section-heading">
     <h2>{{-section.settings.subheading}} 
          <a class="corner-view-all" href="{{section.settings.link}}">{{section.settings.button}}</a>
    </h2>
  </div>
  {%- if section.blocks.size > 0  -%}
  <div class="yv-blogs-wrapper"> 
    <div class="row align-items-center">       
      <div class="col-12 col-sm-12 col-md-6 col-lg-5">
            {%- for block in section.blocks limit:1 -%}
            {%- assign article = block.settings.article -%}
        <div class="yv-blog-card featured-blog">
          <a href="{{-article.url-}}" class="yv-blog-card-img">                    
            {%- if article.image != blank -%}
            {%- include 'lazy-image' image:article.image  -%}
            {%- else -%}
            {%- render 'placeholder' %}
            {%- endif -%}
          </a>
          <div class="yv-blog-card-text">
            <h4 class="yv-blog-title" >{{article.title}}</h4>
            <a href="{{-article.url-}}" class="yv-blog-link">Read More</a>
          </div>
          <span class="yv-blog-date">{{article.published_at | date: "%B %d, %Y" }}</span>
        </div>
            {%- endfor -%}
      </div>
      {%- if section.blocks.size > 1  -%}
      <div class="col-12 col-sm-12 col-md-6 col-lg-7">
        <div class="row small-blog-outer">
            {%- for block in section.blocks offset:1 -%}
            {%- assign article = block.settings.article -%}
          <div class="col-12 col-sm-12 col-md-6 ">
            <div class="yv-blog-card">
              <h3 class="yv-blog-title" href="#">{{article.title}}</h3>
              <a href="{{-article.url-}}" class="yv-blog-card-img">                    
                {%- if article.image != blank -%}
                {%- include 'lazy-image' image:article.image  -%}
                {%- else -%}
                {%- render 'placeholder' %}
                {%- endif -%}
                <span class="yv-blog-date">{{article.published_at | date: "%B %d, %Y" }}</span>
              </a>
              <div class="yv-blog-card-text">
                <p class="yv-blog-desc">{{article.excerpt_or_content | truncatewords:30}}</p>
                <a href="{{-article.url-}}" class="yv-blog-link">Read More</a>
              </div>
            </div>
          </div>
            {%- endfor -%}
        </div>
      </div>
      {%- endif -%}
    </div>
  </div>
  {%- endif -%}
</div>

{% schema %}
{
    "name": "featured-blogs-2",
    "tag": "section",
    "class":"blog-post featured-articles-section",
    "max_blocks":3,
    "settings": [
      {
        "type": "text",
        "id": "subheading",
        "default": "Add Blog posts here",
        "label": "t:sections.featured-article.settings.subheading.label"
      },
      {
        "type": "text",
        "id": "heading",
        "default": "Posts",
        "label": "t:sections.featured-article.settings.heading.label"
      },
      {
        "type": "text",
        "id": "button",
        "default": "View all",
        "label": "t:sections.featured-article.settings.button.label"
      },
      {
        "type": "url",
        "id": "link",
        "label": "t:sections.featured-article.settings.link.label"
      },
      {
        "type": "color",
        "id": "color",
        "label": "t:sections.featured-article.settings.color.label",
        "default":"#ffffff"
      },
      {
        "type": "range",
        "id": "top_space_desktop",
        "label": "t:sections.featured-article.settings.top_space_desktop.label",
        "default":40,
        "min":0,
        "max":60,
        "step":5
      },
      {
        "type": "range",
        "id": "bottom_space_desktop",
        "label": "t:sections.featured-article.settings.bottom_space_desktop.label",
        "default":0,
        "min":0,
        "max":60,
        "step":5
      },
      {
        "type": "range",
        "id": "top_space_mobile",
        "label": "t:sections.featured-article.settings.top_space_mobile.label",
        "default":30,
        "min":0,
        "max":60,
        "step":5
      },
      {
        "type": "range",
        "id": "bottom_space_mobile",
        "label": "t:sections.featured-article.settings.bottom_space_mobile.label",
        "default":30,
        "min":0,
        "max":60,
        "step":5
      }
    ],
    "blocks":[
      {
        "type":"article",
         "name":"t:sections.featured-article.blocks.article.name",
         "settings":[
          {
          "type":"article",
          "id":"article",
          "label": "t:sections.featured-article.blocks.article.settings.article.label"
          }
           ]
       }
         ],
    "presets": [
      {
        "name": "featured-blogs-2",
        "category": "Article",
        "blocks": [
          {
            "type": "article"
          },
          {
            "type": "article"
          },
          {
            "type": "article"
          }
        ]
      }
    ]
  }
  {% endschema %}